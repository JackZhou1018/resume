# 部署上线说明

这个主页是纯静态网站（`index.html` + `style.css` + `script.js`），零依赖，两个平台都能免费秒级上线。

## 方式一：GitHub Pages（推荐，最省事）

1. 注册/登录 [github.com](https://github.com)，点右上角 **+** → **New repository**
2. 仓库名随意（如 `resume`），选 **Public**，创建
3. 进入仓库，点 **uploading an existing file**（上传已有文件）
4. 把这三个文件拖进去，点 **Commit changes**：
   - `index.html`
   - `style.css`
   - `script.js`
5. 点仓库 **Settings** → 左侧 **Pages**
6. **Branch** 选 `main`，目录选 `/ (root)`，点 **Save**
7. 等 1-2 分钟，页面顶部会显示网址：
   ```
   https://你的用户名.github.io/resume/
   ```
   这就是你的在线简历了 🎉

> 之后改内容：直接替换文件重新 commit，几秒后自动更新。

## 方式二：Vercel（更简单，还能绑自定义域名）

1. 打开 [vercel.com](https://vercel.com)，用 GitHub 账号登录
2. 点 **Add New → Project**，选择你刚建的那个 GitHub 仓库
3. 一路 Next，默认配置即可，点 **Deploy**
4. 秒级生成网址：`https://resume-xxx.vercel.app`

## 方式三：本地临时预览（不用部署）

直接双击 `index.html`，或命令行：
```bash
cd "个人简历" && python3 -m http.server 8080
# 浏览器打开 http://localhost:8080
```

---

### 换头像
把你的透明 PNG 放到本目录，改名 `avatar.png`，然后编辑 `index.html` 里 hero 处那一行：
```html
<div class="holo-avatar"><img src="avatar.png" alt="头像"></div>
```
（把原来的 `<!-- <img ...> -->张` 换成上面这行即可）
